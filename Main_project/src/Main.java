public class Main {
    public static void main(String[] args) {
        ConsoleController con = new ConsoleController();
        con.whenRunClicked();
        con.whenRunClicked();
        con.whenRunClicked();
        con.whenRunClicked();
        con.whenRunClicked();
        con.whenRunClicked();
        con.whenRunClicked();
        con.whenRunClicked();
    }
}
